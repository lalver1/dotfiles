- defaults:
    link:
      create: true
      force: true
      relink: true

- clean: ["~"]

- link:
    ~/.shell:
    ~/.bash_logout:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.editorconfig:

- shell:
    - command: bash aliases.sh
      stderr: true
      stdout: true
    - command: bash vscode/install-extensions.sh
      stderr: true
      stdout: true